[package]
name = "orbs-tee-protocol"
version = "0.1.0"
edition = "2021"
description = "ORBS TEE Protocol - Shared message types for host-enclave communication"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
